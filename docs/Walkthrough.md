# Walkthrough

## Video

**Link:** _TODO — add Loom/YouTube link after recording_
**Length:** < 5 minutes

## What the video covers

1. Starting LocalStack and running `terraform apply`
2. Running `janitor.sh --dry-run` and walking through a finding in `report.json`
3. One design decision I'm proud of — the index-loop pattern to avoid Bash subshell scoping
4. One thing I would change — provisioning the Elastic IP in Terraform instead of relying on a LocalStack leftover

## Transcript

_TODO — add transcript after recording_